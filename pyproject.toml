[project]
name = "llm-document-intelligence"
version = "0.1.0"
description = "Production-lean document intelligence with LLMs: schema extraction, caching, retries, tracing."
requires-python = ">=3.10"
